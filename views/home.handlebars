<div class="posts">
  {{#each posts}}
  <article class="card">
    <div class="blog-posts">
      <h2>
        <a href="/post/{{id}}">{{title}}</a>
      </h2>
    </div>
    <div class="card-body">
      {{body}}
    </div>
    <div>
        Posted by {{User.username}} on {{format_date createdAt}}
      </div>
  </article>
  {{/each}}
</div>
